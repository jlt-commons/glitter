# Third-party code

glitter's own code is licensed under the Eclipse Public License 2.0 (see
LICENSE, Copyright 2026 Burin Choomnuan); it was MIT until 2026-09-05. The
vendored third-party code keeps the license it came with, which EPL 2.0 does
not change. This file is the authoritative, file-by-file record of what came
from where, and carries the full text of each upstream license, which applies
to those files in addition to glitter's own.

The Replicant-ported reconciler and glitter-agnostic nexus port that used
to be recorded here moved to
[glitter-core](https://github.com/jlt-commons/glitter-core) and
[nexus](https://github.com/jlt-commons/nexus) respectively — see their own
`NOTICE` files.

## glimmer

The following files under `src/glitter/` are forked from
[glimmer](https://github.com/jolt-lang/glimmer), Copyright 2026 Dmitri Sotnikov
(`Yogthos`), published under the `jolt-lang` organization — a DIFFERENT author
from glitter's. Authorship verified from git history on `upstream/main`: 29
commits, 25 as `Yogthos` and 4 as `Dmitri Sotnikov` — two distinct git
identities, different names and different emails; widely identified elsewhere
as the same person, but git history alone doesn't establish that, and it
doesn't need settling either way since neither identity is glitter's author —
zero by anyone else.

**Upstream ships no LICENSE file.** Absent a license, default copyright reserves
all rights — no grant has been made. This section records accurate provenance,
not a claimed permission. The same author licenses the sibling glimmer-gl under
Apache-2.0, so this appears to be an upstream oversight rather than a deliberate
reservation; that is an observation, not a substitute for a license.

- `src/glitter/ffi.clj` — `src/glimmer/ffi.clj` (+ g-signal-handler-disconnect, gtk-box-insert-child-after, gtk-label-get-text, gtk-widget-get-prev-sibling, gtk-scale-new-with-range, gtk-scale-set-digits, gtk-scale-set-draw-value, gtk-range-set-value, gtk-range-get-value, gtk-range-set-range, gtk-range-set-increments, gtk-widget-add-css-class, gtk-widget-remove-css-class, gtk-widget-has-css-class, gtk-spinner-new, gtk-spinner-set-spinning, gtk-spinner-get-spinning, gtk-progress-bar-new, gtk-progress-bar-set-fraction, gtk-progress-bar-set-text, gtk-progress-bar-set-show-text, gtk-progress-bar-get-fraction, gtk-image-new, gtk-image-new-from-icon-name, gtk-image-new-from-file, gtk-image-set-from-icon-name, gtk-image-set-from-file, gtk-image-set-pixel-size, gtk-image-get-icon-name, gtk-toggle-button-new, gtk-toggle-button-new-with-label, gtk-toggle-button-set-active, gtk-toggle-button-get-active, gtk-level-bar-new, gtk-level-bar-set-value, gtk-level-bar-set-min-value, gtk-level-bar-set-max-value, gtk-level-bar-set-inverted, gtk-level-bar-get-value, gtk-link-button-new, gtk-link-button-new-with-label, gtk-link-button-set-uri, gtk-link-button-get-uri, gtk-widget-activate, gtk-switch-new, gtk-switch-set-active, gtk-switch-get-active, gtk-widget-get-parent, gtk-button-get-label, gtk-revealer-new, gtk-revealer-set-child, gtk-revealer-set-reveal-child, gtk-revealer-get-reveal-child, gtk-revealer-get-child-revealed, gtk-revealer-set-transition-type, gtk-revealer-get-transition-type, gtk-revealer-set-transition-duration, gtk-revealer-get-transition-duration, gtk-center-box-new, gtk-center-box-set-start-widget, gtk-center-box-get-start-widget, gtk-center-box-set-center-widget, gtk-center-box-get-center-widget, gtk-center-box-set-end-widget, gtk-center-box-get-end-widget, gtk-spin-button-new-with-range, gtk-spin-button-set-range, gtk-spin-button-set-value, gtk-spin-button-get-value, gtk-spin-button-set-digits, gtk-spin-button-set-increments, gtk-list-box-new, gtk-list-box-append, gtk-list-box-remove, gtk-list-box-insert, gtk-list-box-get-selected-row, gtk-list-box-row-get-index, gtk-list-box-select-row, gtk-password-entry-new, gtk-password-entry-set-show-peek-icon, gtk-password-entry-get-show-peek-icon, gtk-search-entry-new, gtk-search-entry-set-search-delay, gtk-search-entry-get-search-delay, gtk-expander-new, gtk-expander-set-label, gtk-expander-get-label, gtk-expander-set-expanded, gtk-expander-get-expanded, gtk-expander-set-child, gtk-paned-new, gtk-paned-set-start-child, gtk-paned-get-start-child, gtk-paned-set-end-child, gtk-paned-get-end-child, gtk-paned-set-position, gtk-paned-get-position, gtk-aspect-frame-new, gtk-aspect-frame-set-child, gtk-aspect-frame-get-child, gtk-aspect-frame-set-xalign, gtk-aspect-frame-get-xalign, gtk-aspect-frame-set-yalign, gtk-aspect-frame-get-yalign, gtk-aspect-frame-set-ratio, gtk-aspect-frame-get-ratio, gtk-aspect-frame-set-obey-child, gtk-aspect-frame-get-obey-child, gtk-calendar-new, gtk-calendar-select-day, gtk-calendar-get-date, g-date-time-new-local, g-date-time-get-year, g-date-time-get-month, g-date-time-get-day-of-month, g-date-time-unref, gtk-overlay-new, gtk-overlay-set-child, gtk-overlay-get-child, gtk-overlay-add-overlay, gtk-overlay-remove-overlay, gtk-flow-box-new, gtk-flow-box-append, gtk-flow-box-remove, gtk-flow-box-insert, gtk-flow-box-child-get-index, gtk-picture-new, gtk-picture-new-for-filename, gtk-picture-set-filename, gtk-picture-set-content-fit, gtk-picture-get-content-fit, gtk-picture-set-can-shrink, gtk-picture-get-can-shrink, gtk-picture-set-alternative-text, gtk-picture-get-alternative-text, gtk-editable-label-new, gtk-editable-label-get-editing, gtk-editable-label-start-editing, gtk-editable-label-stop-editing, gtk-notebook-new, gtk-notebook-append-page, gtk-notebook-insert-page, gtk-notebook-remove-page, gtk-notebook-page-num, gtk-notebook-set-current-page, gtk-notebook-get-current-page, gtk-scale-button-new, gtk-scale-button-set-value, gtk-scale-button-get-value, gtk-widget-get-visible, gtk-inscription-new, gtk-inscription-get-text, gtk-inscription-set-text, gtk-inscription-get-text-overflow, gtk-inscription-set-text-overflow, gtk-search-bar-new, gtk-search-bar-set-child, gtk-search-bar-get-child, gtk-search-bar-set-search-mode, gtk-search-bar-get-search-mode, gtk-search-bar-set-show-close-button, gtk-search-bar-get-show-close-button, gtk-header-bar-new, gtk-header-bar-set-title-widget, gtk-header-bar-get-title-widget, gtk-header-bar-pack-start, gtk-header-bar-remove, gtk-header-bar-set-show-title-buttons, gtk-header-bar-get-show-title-buttons, gtk-action-bar-new, gtk-action-bar-pack-start, gtk-action-bar-set-center-widget, gtk-action-bar-get-center-widget, gtk-action-bar-remove, gtk-action-bar-set-revealed, gtk-action-bar-get-revealed, gtk-menu-button-new, gtk-menu-button-set-label, gtk-menu-button-get-label, gtk-menu-button-set-popover, gtk-menu-button-get-popover, gtk-popover-new, gtk-popover-set-child, gtk-popover-get-child, gtk-popover-set-has-arrow, gtk-popover-get-has-arrow, gtk-popover-set-autohide, gtk-popover-get-autohide, gtk-popover-popup, gtk-popover-popdown, gtk-window-handle-new, gtk-window-handle-set-child, gtk-window-handle-get-child, gtk-stack-new, gtk-stack-add-child, gtk-stack-add-named, gtk-stack-remove, gtk-stack-get-child-by-name, gtk-stack-set-visible-child-name, gtk-stack-get-visible-child-name, gtk-string-list-new, gtk-string-list-append, gtk-drop-down-new, gtk-drop-down-set-model, gtk-drop-down-set-selected, gtk-drop-down-get-selected, gtk-grid-new, gtk-grid-attach, gtk-grid-remove, gtk-grid-get-child-at, gtk-grid-set-row-spacing, gtk-grid-get-row-spacing, gtk-grid-set-column-spacing, gtk-grid-get-column-spacing — round 11 — plus round 11 bug-fix bindings gtk-checkbutton-set-label, gtk-scale-button-get-adjustment, gtk-adjustment-configure, gtk-adjustment-get-lower, gtk-adjustment-get-upper, gtk-adjustment-get-step-increment, gtk-range-get-adjustment, gtk-spin-button-get-adjustment, gtk-widget-get-sensitive — added while verifying examples/glitter/crud.clj, the first live check of :sensitive's own read-back)
- `src/glitter/widget.clj` — `src/glimmer/widget.clj` (+ insert-child-after!, signal-name, signal-value-fn, suppressing?, set-scale-value!, scale-spec/:scale widget entry, :on-value-changed signal, spinner-spec/:spinner, progress-bar-spec/:progress-bar, image-spec/:image widget entries (all three display-only, no signal), set-toggle-button-active!, toggle-button-spec/:toggle-button widget entry (reuses :on-toggled verbatim), level-bar-spec/:level-bar widget entry (display-only, no signal), link-button-spec/:link-button widget entry (reuses :on-click verbatim), set-switch-active!, switch-spec/:switch widget entry, :on-state-set signal, revealer-spec/:revealer widget entry (display-only, single-child container), center-box-spec/:center-box widget entry + center-box-append-child!/center-box-remove-child!/center-box-replace-child!/center-box-insert-after! (a genuinely new container strategy — 3 fixed named slots, not an ordered list), set-spin-button-value!/spin-button-spec/:spin-button widget entry, list-box-spec/:list-box widget entry + list-box-row-of/list-box-remove-child!/list-box-replace-child!/list-box-index-after/list-box-insert-after!/list-box-reorder-child!/list-box-selected-index, :on-row-selected/:on-row-activated signals, password-entry-spec/:password-entry + search-entry-spec/:search-entry widget entries (both reuse :entry's GtkEditable-delegate "changed" signal name; :search-entry also gets its own genuinely new :on-search-changed signal), set-expander-expanded!/expander-spec/:expander widget entry (single-child container, no dedicated signal of its own — :on-expanded watches "notify::expanded" instead), set-paned-position!/paned-spec/:paned widget entry + paned-append-child!/paned-slot-setter/paned-remove-child!/paned-replace-child!/paned-insert-after! (a second new 2-named-slot container strategy — :on-position-changed watches "notify::position"), aspect-frame-spec/:aspect-frame widget entry (single-child container, plain floats/bool ctor params), calendar-date=/set-calendar-date!/calendar-spec/:calendar widget entry + :on-day-selected signal (this project's first GDateTime-refcounted value type), overlay-spec/:overlay widget entry + overlay-append-child!/overlay-remove-child!/overlay-replace-child!/overlay-insert-after! (a third new container strategy — 1 main slot + N unenumerable overlay children), flow-box-spec/:flow-box widget entry + flow-box-child-of/flow-box-index-after/flow-box-insert-after!/flow-box-replace-child!/flow-box-reorder-child! + :on-child-activated signal (no value-fn — see the spec's own docstring); behavioural deviations: (1) replace-child!'s :box branch captures old-child's previous sibling via gtk_widget_get_prev_sibling and re-inserts with gtk_box_insert_child_after, where glimmer used gtk_box_remove + gtk_box_append — append always lands at the END of the box, silently relocating any non-final child — see the fn's docstring; (2) signal-value is keyed by [tag gtk-signal-name], not bare signal name — :spin-button and :scale emit the identical "value-changed" signal but need different getters (and :password-entry/:search-entry each need their own "changed" entry despite reusing :entry's signal NAME), found live before it shipped and broke :scale (and, separately, before it shipped :password-entry with a permanently-nil dispatched value); (3) insert-child-after!/reorder-child! are no longer :box-only no-ops — :list-box/:flow-box needed both genuinely implemented (a plain non-keyed tag swap goes through insert-child-after!, and leaving it a no-op desyncs glitter.gtk's own child bookkeeping from live GTK state), :center-box/:paned/:overlay get insert-child-after! but reorder-child! stays a structural no-op for all three; (4) list-box-remove-child!/list-box-replace-child!/list-box-reorder-child! suppress on the list-box widget around gtk_list_box_remove — removing the currently-selected row fires a real, synchronous "row-selected(NULL)" GTK signal that would otherwise reach app dispatch; (5) unlike gtk_list_box_remove, gtk_flow_box_remove accepts either the wrapped GtkFlowBoxChild or the plain widget directly (confirmed by reading its C body, not assumed from the sibling relationship), so flow-box-* container functions don't need a list-box-row-of-style unwrap helper for removal — see docs/guide/gtk-widget-layer.md for all five); round 9 additions: editable-label-spec/:editable-label widget entry (reuses set-entry-text!/:entry's "changed" signal NAME — a THIRD GtkEditable-delegate reuse — needs its own [:editable-label "changed"] signal-value entry), set-editable-label-editing!, picture-spec/:picture widget entry (display-only, no signal), notebook-spec/:notebook widget entry + notebook-append-child!/notebook-remove-child!/notebook-replace-child!/notebook-index-after/notebook-insert-after!/notebook-reorder-child! (children are NOT auto-wrapped, unlike :list-box/:flow-box — gtk_notebook_page_num recovers a child's page index directly), set-notebook-current-page!, :on-switch-page signal, scale-button-spec/:scale-button widget entry, set-scale-button-value!, signal-value entries [:editable-label "changed"] and [:scale-button "value-changed"]; behavioural deviations: (6) :editable-label repeated the exact :password-entry near-miss from round 7 — a comment said its own signal-value entry would be added "proactively," and it was still missed on the first pass, caught only by live probe testing, not the comment; (7) investigating that miss surfaced a general (not glitter-specific) GtkEditable finding — gtk_editable_set_text is delete-then-insert, two separate mutations, so replacing already-empty text fires "changed" once but replacing non-empty text fires it twice, confirmed by reading gtk_editable_set_text's and gtk_text_delete_text's C bodies directly; affects :entry/:password-entry/:search-entry/:editable-label alike; (8) :notebook's "switch-page" cannot use the shared value-fn/dispatch! path at all — gtk_notebook_switch_page (which emits it) only reads the OLD current page, the actual page assignment happens in the signal's own default class handler which runs AFTER glitter's connected handler, so set-event-handler reads page-num from its own raw signal argument instead, verified both by reading the C source and by an empirical probe comparing the raw value against a same-tick (confirmed stale) getter read; (9) GTK auto-selects the first page added to an empty notebook (confirmed via gtk_notebook_insert_page's C body), so mounting a :notebook with initial children genuinely dispatches "switch-page" as a mount-time side effect, before any real interaction — see docs/guide/gtk-widget-layer.md for all four; round 10 additions: inscription-spec/:inscription + search-bar-spec/:search-bar widget entries (both entirely display/props-driven, no signal, no glitter.gtk changes needed), header-bar-spec/:header-bar + action-bar-spec/:action-bar widget entries + header-bar-append-child!/header-bar-remove-child!/header-bar-replace-child!/header-bar-insert-after! + action-bar-append-child!/action-bar-remove-child!/action-bar-replace-child!/action-bar-insert-after! (a genuinely new HYBRID container shape — one named title/center-widget slot plus an ordered pack-start list — modeled on overlay-append-child!'s "query GTK's own occupancy" pattern: first hiccup child becomes the title/center-widget if that slot is empty, every later child gets pack_start'd), menu-button-spec/:menu-button + popover-spec/:popover widget entries + set-popover-visible! + :on-closed signal (the FIRST popup surface in this project — :menu-button's ONE hiccup child, if present, is attached via gtk_menu_button_set_popover rather than any append-child!-managed tree-child branch); behavioural deviations: (10) both gtk_header_bar_pack_end and gtk_action_bar_pack_end reverse-accumulate (confirmed independently via each C body — a gtk_box_prepend under one name, a gtk_box_insert_child_after(..., NULL) under the other), so v1 never calls either — only pack_start is bound and used; (11) toggling :header-bar's :show-title-buttons prepends GTK's own native GtkWindowControls widget into the SAME start_box glitter's pack-start children live in (confirmed via create_window_controls's C body), a real GTK behavior asserted explicitly in header_bar_action_bar_smoke.clj rather than avoided; (12) both :on-activate (:menu-button) and :on-closed (:popover) are free reuses of the default 2-arg-void callable shape, confirmed via gtkmenubutton.c's/gtkpopover.c's own g_signal_new calls — the first round needing zero glitter.gtk changes for any new widget's signal wiring — see docs/guide/gtk-widget-layer.md for all three); round 11 additions: a ctor/apply architecture audit (a probe confirmed :ctor's props argument is ALWAYS empty at the real create-element call site) found and fixed four real, previously-shipped bugs — gtk-checkbutton-set-label added to checkbutton-spec's :apply (its :label was read in :ctor but never applied at all); scale-button-spec's :min/:max/:step given the same fix via gtk-scale-button-get-adjustment + gtk-adjustment-configure (GtkScaleButton has no direct set-range call); scale-spec's and spin-button-spec's :min/:max fixed to read the CURRENT adjustment's lower/upper via gtk-range-get-adjustment/gtk-spin-button-get-adjustment as fallbacks instead of hardcoded 0/100 (both were silently clobbering each other across separate single-key re-renders, confirmed live to fire even at initial mount); window-spec's :width/:height found to have the identical clobbering shape but left unfixed (documented v1 gap — its getter needs OUT-PARAMETER FFI marshalling); set-stack-visible-child-name!/stack-spec/:stack widget entry + the new :on-visible-child-changed signal (GTK auto-selects the first added visible child — a THIRD mount-time-auto-dispatch instance after :notebook — and :apply running before the reconciler appends children means an initial non-default :visible-child-name never takes effect at mount; the resulting GTK warning is silenced via a gtk-stack-get-child-by-name existence guard, the underlying timing gap left as a documented v1 gap); set-drop-down-selected!/drop-down-build-model!/drop-down-spec/:drop-down widget entry + the new :on-selected-changed signal (the first "choose from options" widget, built on an incrementally-constructed GtkStringList via gtk-string-list-new/gtk-string-list-append, deliberately sidestepping gtk_drop_down_new_from_strings' raw C-string-array argument; both :notify::selected and :activate turned out to be free reuses of shapes already generalized elsewhere, needing zero new glitter.gtk callable-shape code); window-handle-spec/:window-handle widget entry (single-child, no signal — its first live smoke run caught a bug in THIS round's own new code, not a pre-existing one: append-child!/remove-child!/replace-child! were entirely missing a :window-handle case branch, so the widget mounted with no exception but silently had no child); grid-attach!/stack-append-child!/grid-spec/:grid widget entry (the first container whose child placement is driven entirely by the child's own hiccup props — :grid-column/:grid-row/:grid-column-span/:grid-row-span read via the new :glitter/structural-props mechanism, see glitter.gtk below); behavioural deviations: (13) append-child!/insert-child-after!/replace-child! each gained a new optional trailing structural-props argument, threaded from glitter.gtk (see below), and grid-attach!/stack-append-child! are the only consumers; (14) a probe found that a NAMESPACED keyword prop (e.g. :grid/column) is silently dropped by glitter.core's own set-attr/update-attr, upstream of every IRender backend — this is why :grid's/:stack's structural props use plain, hyphenated keys (:grid-column, :stack-name) instead of the more idiomatic namespaced form — see docs/guide/gtk-widget-layer.md for all of the above); post-round-11 fix, found while building examples/glitter/crud.clj (a port of the 7GUIs CRUD task): (15) list-box-reorder-child!/flow-box-reorder-child! were reusing a widget pointer GTK had already disposed — gtk_list_box_remove/gtk_flow_box_remove dispose the now-unreferenced wrapping row/child, and BOTH wrappers' own dispose handlers unparent (and, with nothing else referencing it, finalize) their own child in turn (confirmed by reading gtk_list_box_row_dispose's and gtk_flow_box_child_dispose's C bodies directly) — fixed by bracketing the remove-then-reinsert in both functions with g-object-ref-sink/g-object-unref, the first actual call site for either binding in this codebase (both were already bound, inherited from glimmer's fork, but never previously used) — see docs/guide/gtk-widget-layer.md)
- `src/glitter/genum.clj` — `src/glimmer/genum.clj`
- `src/glitter/app.clj` — adapted from `src/glimmer/core.clj`'s non-reactive app-loop functions (post-to-gui, on-gui, run*, run); mount/unmount!/reload!/live-root/make-rerender-watcher are NOT ported (glimmer-reconciler-specific)

<!-- appended to by each forking task -->

## New code

The following files are original to glitter:

- `src/glitter/gtk.clj` — IRender/IMemory GTK4 backend and state-atom mount/render wiring. `set-event-handler` was generalized beyond the uniform `[:pointer :pointer] :void` foreign-callable shape to also support `GtkSwitch`'s `"state-set"` (`[:pointer :int :pointer] :int`) and `GtkListBox`'s `"row-selected"`/`"row-activated"` (`[:pointer :pointer :pointer] :void`, a third distinct shape, also reused for free by `:expander`'s `"notify::expanded"`, `:paned`'s `"notify::position"`, and `:flow-box`'s `"child-activated"` — all four share the identical shape) — branches explicitly on GTK signal name at separate literal `foreign-callable` call sites (jolt's `argtypes`/`rettype` must be compile-time literals, verified live; a data-driven table lookup does not work) — see `docs/guide/gtk-widget-layer.md`. Round 9 added two more branches: `GtkScaleButton`'s `"value-changed"` (`[:pointer :double :pointer] :void`, a FIFTH shape, sharing `:scale`'s/`:spin-button`'s signal NAME but not their shape — the first branch here that also checks the widget's own `:tag`, not just the signal name) and `GtkNotebook`'s `"switch-page"` (`[:pointer :pointer :uint :pointer] :void`, a SIXTH shape, and the first that reads its own raw signal argument instead of going through the shared `dispatch!`/`value-fn` path at all — see `gtk-widget-layer.md` for why the getter-based pattern is unsafe there specifically). Round 10 added no branches here at all — `:menu-button`'s `"activate"` and `:popover`'s `"closed"` both turned out to be free reuses of the default `[:pointer :pointer] :void` shape, the first round where every new widget's signal wiring needed zero changes to this file. Round 11 added `structural-child-props`/`structural-child-prop?` (a new `#{:grid-column :grid-row :grid-column-span :grid-row-span :stack-name}` set) and special-cased them in `set-attribute`/`remove-attribute` — stashing matches on the CHILD's own `el` atom under `:glitter/structural-props` instead of routing through `glitter.widget/apply-props!`, since no widget's own `:apply` closure can know what parent it's about to be attached to. `append-child`, `insert-before`'s fresh-insert branch, and `replace-child` all thread `(:glitter/structural-props @child-node)` through to `glitter.widget`'s container-management functions as a new optional trailing argument. Added `"notify::visible-child-name"`/`"notify::selected"` to the existing 3-arg-void signal-name set (`:stack`'s and `:drop-down`'s property-change signals — both free reuses, no new `foreign-callable` branch needed). This mechanism exists because a probe confirmed a NAMESPACED keyword prop (`:grid/column`) is silently dropped by `glitter.core`'s own `set-attr`/`update-attr`, upstream of `IRender` entirely — see `docs/guide/gtk-widget-layer.md`

## b12n-adk-clj / b12n-raylib-jlt

Same author as glitter. Neither carries a license file, so there is no
attribution obligation; both are listed here for provenance only.
b12n-raylib-jlt is public, and has since moved to
(https://github.com/jlt-commons/raylib-jlt);
b12n-adk-clj is private, so its copy cannot be linked from here.

- `scripts/check_positional_args.clj` — `scripts/check_positional_args.clj`
  from b12n-adk-clj, `source-dirs` retargeted to `src/glitter`; one
  behavioural fix: `file-pattern` changed from the original's `"**/*.clj"`
  to `"{*,**/*}.clj"` — verified live that babashka.fs/glob's `**` requires
  at least one directory level, so the original pattern silently matches
  only files in subdirectories and misses every file sitting directly in
  `source-dirs` (glitter's `src/glitter` is flat, so the original pattern
  would have found zero of its 17 files; the same bug affects b12n-adk-clj's
  own copy, which has both flat and nested source files).
- `.clj-kondo/hooks/jolt_ffi.clj` — `.clj-kondo/hooks/jolt_ffi.clj` from
  b12n-raylib-jlt (same `jolt.ffi/defcfn` macro, same false-positive problem);
  one behavioural deviation: `:pointer` return values map to a number, not
  `nil` — glitter.ffi's own ns docstring states pointers are "plain machine
  addresses (jolt numbers)", and glitter.genum/glitter.widget call `zero?`
  directly on `:pointer`-typed return values, which trips a spurious
  `type-mismatch` against a nil-typed stub (see the hook's docstring).
